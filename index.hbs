{{!< default}}

<div id="latest-post" class="container">
    {{#foreach posts}}
        {{#if @first}}
            {{> post-content}}
        {{/if}}
    {{/foreach}}
</div>

<div id="post-index" class="container">
    <ol class="post-list">
        {{#foreach posts}}
            <li class="post-stub {{post_class}}" >
                <a class="js-ajax-link"  title="{{#if featured}}Featured: {{/if}}{{title}}" href="{{url}}">
                    <h4 class="post-stub-title">{{title}}</h4>

                    <time class="post-stub-date" datetime="{{published_at}}">{{date format="MMM Do, YY"}}</time>

                    {{#if featured}}
                        <i class="post-stub-tag fa fa-heart"></i>
                    {{/if}}
                </a>
            </li>
        {{/foreach}}
    </ol>

    <div class="post-navigation">
        {{pagination}}
    </div>
</div>